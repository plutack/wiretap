# Wiretap documentation

The public documentation site is built with Astro Starlight.

```sh
cd docs
npm install
npm run dev
```

Run `npm run build` before publishing. User-facing pages live under
`src/content/docs`; historical release notes in `releases` are retained for
the GitHub release workflow and are not part of Starlight's content tree.
