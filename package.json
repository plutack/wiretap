{
  "name": "wiretap",
  "version": "0.0.0",
  "private": true,
  "description": "Dev-time package manifest. wiretap itself is a Go project; this file only carries commitlint for local pre-commit linting (and future dev-time JS tooling). Run `npm install` once per clone to populate ./node_modules.",
  "devDependencies": {
    "@commitlint/cli": "^21.2.0",
    "@commitlint/config-conventional": "^21.2.0"
  },
  "scripts": {
    "commitlint": "commitlint --edit"
  }
}